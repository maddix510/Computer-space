# Computer Space — 1971 arcade remake

A small browser remake of the 1971 arcade classic **Computer Space** built with HTML/CSS/JS.  
Designed to be run in GitHub Codespaces or any static web server.

## Run locally (in Codespaces)
1. Create a new repository and add these files (or copy this folder).
2. Open the repo in a Codespace.
3. Start a static server (example using Python):
